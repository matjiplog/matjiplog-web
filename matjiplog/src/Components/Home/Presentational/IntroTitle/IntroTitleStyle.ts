import styled from "styled-components";

import { whiteFont } from '../../../../styles/fontColor';

export const Title = styled.h1`
  position: absolute;
  top: 50%;
  right: 10%;
  font-size: 4.5rem;
  color: ${whiteFont};
  z-index: 999;
`;
